<div class="card border-info mb-3" style="max-width: 40rem;">
  <div class="card-header">Comment by: <%= comment.author_name %></div>
  <div class="card-body">
    <p class="comment card-text"><%= comment.body %></p>
    <p class="card-footer muted-text">Posted <%= distance_of_time_in_words(comment.article.created_at, comment.created_at) %> later</p>
  </div>
</div>
